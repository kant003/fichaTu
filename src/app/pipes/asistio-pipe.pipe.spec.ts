import { AsistioPipePipe } from './asistio-pipe.pipe';

describe('AsistioPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AsistioPipePipe();
    expect(pipe).toBeTruthy();
  });
});
