import { BooleanoPipePipe } from './booleano-pipe.pipe';

describe('BooleanoPipePipe', () => {
  it('create an instance', () => {
    const pipe = new BooleanoPipePipe();
    expect(pipe).toBeTruthy();
  });
});
